# InboxValid MCP Server Configuration
NODE_ENV=production
LOG_LEVEL=info

# Optional: Set custom DNS lookup timeout (milliseconds)
DNS_TIMEOUT_MS=3000

# Optional: Max retries for DNS resolution backoff
DNS_MAX_RETRIES=3
